# Dev Test Users (Local Only)

These keypairs are used by backend `DEV_LOCAL_SIGNER_BYPASS` mode for
localhost integration tests that bypass Privy signing.

- `user1.json` -> Bearer `user1` / `bettor1`
- `user2.json` -> Bearer `user2` / `bettor2`

Do not use these in production.
