# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/
build/
*.egg

# Large pre-built web app (build separately)
streaming/web/
streaming/output/

# Training scan binary data
training/data/position_scan/
training/data/rollouts/
training/data/bridge/
training/data/cache/
training/data/reverse/

# Environment
.env
*.env.local

# Logs
logs/*.log
*.log

# Database
*.db
*.sqlite3

# ML model temporary outputs
hls_output/
vod_archive/
/tmp/

# Emulator runtime
.m64p/
/training/data/bridge/
/training/data/cache/
/backend/alembic/versions/

# Secrets / keys
*.pem
*.key

# macOS
.DS_Store

# Flutter
streaming/flutter_app/.dart_tool/
streaming/flutter_app/.flutter-plugins
streaming/flutter_app/.flutter-plugins-dependencies
streaming/flutter_app/build/
streaming/flutter_app/.packages
streaming/output/

# Training artifacts (large)
training/data/rollouts/
training/data/checkpoints/
backend/app/agents/checkpoints/*.onnx
backend/app/agents/checkpoints/*.onnx.data

# ROM files (large binary, distribute separately)
*.z64
*.n64
*.rom
*.sfc
*.cue
*.bin

# Alembic auto-generated versions
backend/alembic/versions/

# vendor (compiled binaries)
vendor/mupen64plus-core/build/
vendor/mupen64plus-ui-console/build/
