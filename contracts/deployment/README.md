# Contract Deployment Workspace

- `devnet/` contains devnet deployment scripts and config templates.
- `testing/` contains local and devnet smoke tests for deployment validation.

Primary contract project:

- `contracts/skr_parimutuel_betting`
